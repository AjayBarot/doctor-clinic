<%= form_for(@time_availability) do |f| %>
  <% if @time_availability.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@time_availability.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @time_availability.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :start_time %><br>
    <%= f.datetime_select :start_time %>
  </div>
  <div class="field">
    <%= f.label :end_time %><br>
    <%= f.datetime_select :end_time, default: 1.hours.from_now %>
  </div>
  <%= f.hidden_field :doctor_id, value: current_doctor.id %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
